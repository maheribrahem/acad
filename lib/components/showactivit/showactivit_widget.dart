import '/flutter_flow/flutter_flow_theme.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import 'showactivit_model.dart';
export 'showactivit_model.dart';

class ShowactivitWidget extends StatefulWidget {
  const ShowactivitWidget({super.key});

  @override
  _ShowactivitWidgetState createState() => _ShowactivitWidgetState();
}

class _ShowactivitWidgetState extends State<ShowactivitWidget> {
  late ShowactivitModel _model;

  @override
  void setState(VoidCallback callback) {
    super.setState(callback);
    _model.onUpdate();
  }

  @override
  void initState() {
    super.initState();
    _model = createModel(context, () => ShowactivitModel());

    WidgetsBinding.instance.addPostFrameCallback((_) => setState(() {}));
  }

  @override
  void dispose() {
    _model.maybeDispose();

    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    context.watch<FFAppState>();

    return Switch(
      value: _model.activiValue ??= false,
      onChanged: (newValue) async {
        setState(() => _model.activiValue = newValue);
      },
      activeColor: FlutterFlowTheme.of(context).primary,
      activeTrackColor: FlutterFlowTheme.of(context).accent1,
      inactiveTrackColor: FlutterFlowTheme.of(context).alternate,
      inactiveThumbColor: FlutterFlowTheme.of(context).secondaryText,
    );
  }
}
